# Sticker 0.7.4-rc2.12

运行环境：DSH 0.1.5-rc.2 / web；配套 Core 0.3.12-rc2.29、Bridge 0.4.1-rc2.11。

修复初始化中卸载、晚到 remote 清理和动态依赖替换。默认数据按 profile 隔离；显式 storageDirectory 保留。**旧默认数据必须先核对归属并按 [迁移说明](cli-operations-sticker-storage.md) 离线迁移**，工具保留源文件、原字节备份、outbox 和冻结标记。

125 项回归、类型检查与构建通过；迁移 CLI 已通过合成文件烟测。真实用户数据迁移、安装和 UI 未验收。底层挂载未返回 disposer 前需等待其完成，不宣称可强制取消。
