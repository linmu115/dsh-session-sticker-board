# 原生上下文组合兼容更新

版本 0.7.3-rc2.17，日期 2026-09-15。

本补丁接受 Annotation Core 0.3.12-rc2.11，并按依赖关系接受同批 Lifecycle、Reference Adapter 和 Sticker Board。Suite 的成员清单固定为本批精确版本；既有明确兼容范围保留。

这次改动是 DSH 0.1.5-rc.2 原生 Agent 上下文管理组合的安装兼容性同步。本插件继续使用既有引用、贴纸、桥接或侧聊职责；请求索引、窗口和材料释放由 Core、Maintenance 和 ThoughtDAG 的对应能力提供，不把本补丁解释为托管引擎支持。运行副本安装与实际原生输入释放由联合发布流程单独验证。

检查范围：清单、Bundle 顺序和精确成员一致性；现有来源/回链/挂载行为的重点回归；构建和锁文件一致性。开发依赖继续使用既有明确本地输入，不将旧包锁中的真实摘要伪装成新包。

## 本仓库验证

8 个相关测试文件、56 项通过；清单、Bundle、来源标记、会话入口、笔记关联与 Bridge Client 均通过。构建与类型检查通过。

冻结锁文件的离线一致性检查通过，锁文件无需修改。联合打包前仍需把开发成员链接到本批精确源码，再构建并验证真实实例；以上单仓结果不等同于已经完成运行副本升级。
