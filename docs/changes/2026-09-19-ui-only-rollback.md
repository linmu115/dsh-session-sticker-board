# UI-only rollback

Baseline: `612965b`.

Restore optional projection lookup and maintenance loading/request behavior to the pre-functional-round source. Preserve compact UI, palette, editor and markers. Typecheck/build passed; 8 focused tests passed.

User requested rollback first and requirements confirmation before any new implementation. Candidate only; active installation remains unchanged until normal Launcher stop and backup.
