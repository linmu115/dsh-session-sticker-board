# 贴纸编辑与引用尾标（2026-09-18）

按确认方案实现，未发布或安装到运行实例。原工作树 README/package.json/三项发行合同测试的已有修改保持原样，未纳入本次提交。

浮窗和 sidebar 正文采用同底色、无嵌套输入框的单层样式。色盘收为当前颜色点，展开黄/绿/粉/蓝四种预设；选色通过临时预览事件更新原文高亮，关闭编辑卸载时撤销预览，只有保存才持久化。新建贴纸默认黄色，已有颜色不变。保存改为黑圆白勾，标签折叠，复制/笔记关联/删除/反向链接/同步冲突与恢复入口保留。Markdown Enter 仍换行。

尾标改为 14px 细线引号、22px 轻量点击区；多行采用末行坐标，修正旧的首行纵坐标错位。新建未保存贴纸也有临时高亮预览。

验证：初轮完整 20 文件/123 项通过；最终新增色盘交互及受影响覆盖 4 文件/21 项通过；typecheck/build 通过。实际组件浏览器合成页验证了浮窗、sidebar、四色色盘和颜色保存；发现并修复通用按钮背景覆盖色盘的问题；页面无 error 日志。

一次测试遇到 Core 构建正在清理 lib 的依赖暂不可用；Core 完成后顺序重跑，21 项通过。后续必须先完成 Core 构建，再运行贴纸构建/测试。

边界：未验收实际 sidebar 宿主、真实双链写入/同步冲突，以及多个标记重叠；未改动 Vault、绑定和历史数据。
