# RC2 reference delivery compatibility

Accept the annotation pending-state stream release and its paired bridge packages. This package changes peer compatibility only; reference data and plugin behavior are unchanged.

Validation: package build/pack and strict peers in the isolated RC2 copy profile.
