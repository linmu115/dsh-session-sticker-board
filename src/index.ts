import type { Context } from "./context-types.ts";

export const name = "dsh-session-sticker-board";

export function apply(_ctx: Context): void {
  // The MVP is browser-only; Vault persistence is owned by the Obsidian bridge.
}
